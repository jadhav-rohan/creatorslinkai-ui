import {useCallback,useEffect,useRef,useState} from "react";import {api} from "../api";import {useAuth} from "../context/AuthContext";import {useWorkspace} from "../context/WorkspaceContext";import {useWorkspaceAuthorization} from "../context/WorkspaceAuthorizationContext";import {connectionService} from "../services/connectionService";import {creatorDashboardService} from "../services/creatorDashboardService";
const accountName=a=>a?.username||a?.igUsername||a?.handle||"Instagram account",support=e=>e?.requestId?` Support ID: ${e.requestId}`:"";
function Activity({igUserId,ruleId,token,logout}){const [open,setOpen]=useState(false),[data,setData]=useState(null),[loading,setLoading]=useState(false),[error,setError]=useState(null),request=useRef(0);async function toggle(){if(open){setOpen(false);return}setOpen(true);if(data)return;const id=++request.current;setLoading(true);setError(null);try{const x=await api.getRuleLogs(igUserId,ruleId,token);if(id===request.current)setData(Array.isArray(x)?x:[])}catch(e){if(e.status===401)logout();else if(id===request.current)setError(e)}finally{if(id===request.current)setLoading(false)}}return <div className="mt-4 border-t-2 border-zinc-900 pt-4"><button type="button" onClick={toggle} aria-expanded={open} className="border-2 border-zinc-900 bg-white px-4 py-2 text-sm font-black">{open?"Hide delivery activity":"View delivery activity"}</button>{open&&<div className="mt-4">{loading?<p>Loading delivery activity…</p>:error?<div role="alert" className="border-2 border-red-700 bg-red-50 p-3 text-red-800"><p>Delivery activity could not be loaded.{support(error)}</p><button onClick={()=>{setData(null);setOpen(false)}} className="mt-2 font-black underline">Close and retry</button></div>:!data?.length?<p className="text-sm text-zinc-600">No delivery attempts recorded yet.</p>:<div className="space-y-3">{data.map((log,i)=><article key={log.id||log.commentId||i} className="border-2 border-zinc-900 bg-zinc-50 p-3 text-sm"><dl className="grid gap-2 sm:grid-cols-2"><div><dt className="text-zinc-500">Comment ID</dt><dd className="break-all font-mono">{log.commentId||"—"}</dd></div><div><dt className="text-zinc-500">Commenter scoped ID</dt><dd className="break-all font-mono">{log.commenterScopedId||log.commenterId||"—"}</dd></div><div><dt className="text-zinc-500">Delivery status</dt><dd className="font-black">{log.deliveryStatus||log.status||"—"}</dd></div><div><dt className="text-zinc-500">Attempted</dt><dd>{log.sentAt||log.attemptedAt||log.createdAt?new Date(log.sentAt||log.attemptedAt||log.createdAt).toLocaleString():"—"}</dd></div></dl>{log.errorMessage&&<p className="mt-2 text-red-700">{log.errorMessage}</p>}</article>)}</div>}</div>}</div>}
export default function CreatorAutoDm(){const {token,logout}=useAuth(),{selectedWorkspace,loading:workspaceLoading}=useWorkspace(),{hasPermission,isLoading:permissionsLoading,error:permissionsError}=useWorkspaceAuthorization(),workspaceId=selectedWorkspace?.id||"",workspaceAllowed=["CREATOR","PERSONAL"].includes(selectedWorkspace?.type),canView=hasPermission("AUTO_DM_VIEW"),canEdit=hasPermission("AUTO_DM_EDIT"),[accounts,setAccounts]=useState([]),[accountsLoading,setAccountsLoading]=useState(false),[accountsError,setAccountsError]=useState(null),[selectedId,setSelectedId]=useState(""),[rules,setRules]=useState([]),[rulesLoading,setRulesLoading]=useState(false),[rulesError,setRulesError]=useState(null),[showForm,setShowForm]=useState(false),[form,setForm]=useState({mediaId:"",keyword:"",dmMessage:"",publicReplyMessage:""}),[formError,setFormError]=useState(""),[saving,setSaving]=useState(false),[deleting,setDeleting]=useState(""),[connecting,setConnecting]=useState(false),[notice,setNotice]=useState("");const loadAccounts=useCallback(async signal=>{if(!workspaceId||!workspaceAllowed||!canView)return;setAccountsLoading(true);setAccountsError(null);try{const x=await connectionService.listInstagram(workspaceId,token,signal),items=Array.isArray(x)?x:[];setAccounts(items);setSelectedId(current=>{const candidate=current||sessionStorage.getItem(`creatorAutoDmAccount:${workspaceId}`);return items.some(a=>a.igUserId===candidate)?candidate:items[0]?.igUserId||""})}catch(e){if(e.name==="AbortError")return;if(e.status===401)logout();else setAccountsError(e)}finally{if(!signal?.aborted)setAccountsLoading(false)}},[workspaceId,workspaceAllowed,canView,token,logout]);useEffect(()=>{setAccounts([]);setSelectedId("");setRules([]);const c=new AbortController();loadAccounts(c.signal);return()=>c.abort()},[loadAccounts]);const loadRules=useCallback(async()=>{if(!selectedId)return;setRulesLoading(true);setRulesError(null);try{const x=await api.fetchRules(selectedId,token);setRules(Array.isArray(x)?x:[])}catch(e){if(e.status===401)logout();else setRulesError(e)}finally{setRulesLoading(false)}},[selectedId,token,logout]);useEffect(()=>{setRules([]);setShowForm(false);if(selectedId){sessionStorage.setItem(`creatorAutoDmAccount:${workspaceId}`,selectedId);loadRules()}},[selectedId,workspaceId,loadRules]);async function connect(){if(connecting)return;setConnecting(true);setAccountsError(null);try{const x=await connectionService.connectInstagram(workspaceId,token);window.location.assign(x.authorizationUrl)}catch(e){if(e.status===401)logout();else setAccountsError(e);setConnecting(false)}}async function create(e){e.preventDefault();if(!canEdit||saving)return;const clean={mediaId:form.mediaId.trim(),keyword:form.keyword.trim(),dmMessage:form.dmMessage.trim(),publicReplyMessage:form.publicReplyMessage.trim()};if(!clean.mediaId||!clean.keyword||!clean.dmMessage){setFormError("Target Reel/Post, keyword, and private DM message are required.");return}setSaving(true);setFormError("");try{await api.createRule(selectedId,{...clean,publicReplyMessage:clean.publicReplyMessage||undefined},token);setForm({mediaId:"",keyword:"",dmMessage:"",publicReplyMessage:""});setShowForm(false);creatorDashboardService.invalidate(workspaceId);await loadRules();setNotice("Comment Auto-DM rule created successfully.")}catch(err){if(err.status===401)logout();else setFormError(`${err.message}${support(err)}`)}finally{setSaving(false)}}async function remove(rule){if(!canEdit||deleting||!window.confirm(`Delete the keyword rule “${rule.keyword}”?`))return;setDeleting(rule.id);setRulesError(null);try{await api.deleteRule(selectedId,rule.id,token);creatorDashboardService.invalidate(workspaceId);await loadRules();setNotice("Comment Auto-DM rule deleted.")}catch(e){if(e.status===401)logout();else setRulesError(e)}finally{setDeleting("")}}if(workspaceLoading||permissionsLoading)return <main className="brutal-page min-h-[calc(100vh-82px)] p-6 md:p-8"><div className="mx-auto max-w-6xl brutal-card animate-pulse p-8">Restoring Comment Auto-DM access…</div></main>;if(!workspaceAllowed)return <main className="brutal-page min-h-[calc(100vh-82px)] p-6 md:p-8"><div className="mx-auto max-w-3xl brutal-card p-8"><h1 className="text-3xl font-black">Creator workspace required</h1><p className="mt-3">Comment Auto-DM is available only in Creator and legacy Personal workspaces.</p></div></main>;if(permissionsError||!canView)return <main className="brutal-page min-h-[calc(100vh-82px)] p-6 md:p-8"><div className="mx-auto max-w-3xl brutal-card p-8"><p className="brutal-overline">Access denied</p><h1 className="mt-3 text-3xl font-black">You don’t have permission to view Comment Auto-DM rules.</h1></div></main>;return <main className="brutal-page min-h-[calc(100vh-82px)] p-4 sm:p-6 md:p-8"><div className="mx-auto max-w-6xl"><header className="flex flex-col gap-5 border-b-2 border-zinc-900 pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="brutal-overline">Creator workspace</p><h1 className="mt-2 text-4xl font-black">Comment Auto-DM</h1><p className="mt-2 max-w-3xl text-zinc-600">Send one private-reply attempt when a comment on a selected Reel or post contains your keyword. A public reply is posted only when configured. This does not reply to incoming Instagram conversations.</p></div>{canEdit&&selectedId&&<button onClick={()=>{setFormError("");setShowForm(x=>!x)}} className="brutal-button">{showForm?"Close form":"Create Auto-DM Rule"}</button>}</header><div aria-live="polite">{notice&&<p role="status" className="mt-5 border-2 border-zinc-900 bg-emerald-200 p-3 font-bold">{notice}</p>}</div>{accountsLoading?<div className="brutal-card mt-7 animate-pulse p-8">Loading Instagram Login accounts…</div>:accountsError?<section className="brutal-card mt-7 p-8"><h2 className="text-2xl font-black">Instagram accounts couldn’t be loaded.</h2><p role="alert" className="mt-3 text-red-700">{accountsError.message}{support(accountsError)}</p><button onClick={()=>loadAccounts()} className="brutal-button mt-5">Retry accounts</button></section>:!accounts.length?<section className="brutal-card mt-7 bg-yellow-200 p-8"><h2 className="text-2xl font-black">Connect Instagram to create Comment Auto-DM rules.</h2><p className="mt-3">Connect an Instagram Login account. Insights and analytics snapshots are not required.</p><button onClick={connect} disabled={connecting} className="mt-6 min-h-13 border-2 border-zinc-900 bg-white px-6 py-3 font-black shadow-[4px_4px_0_#18181b]">{connecting?"Opening Instagram…":"Connect Instagram"}</button></section>:<><div className="mt-7 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><label className="font-bold">Instagram account<select value={selectedId} onChange={e=>setSelectedId(e.target.value)} className="brutal-field mt-2 block min-w-64">{accounts.map(a=><option key={a.igUserId} value={a.igUserId}>@{accountName(a)}</option>)}</select></label>{!canEdit&&<p className="border-2 border-zinc-900 bg-sky-100 p-3 font-bold">Read-only access</p>}</div>{showForm&&canEdit&&<form onSubmit={create} className="brutal-card mt-7 bg-yellow-50 p-5 sm:p-7"><h2 className="text-2xl font-black">New keyword rule</h2><p className="mt-2 text-sm text-zinc-600">The keyword is matched against comments on the target Reel/Post. Each matching comment receives one private-reply attempt.</p><div className="mt-6 grid gap-5 sm:grid-cols-2"><label className="block font-bold">Target Reel/Post media ID *<input value={form.mediaId} onChange={e=>setForm(x=>({...x,mediaId:e.target.value}))} required className="brutal-field mt-2 w-full"/></label><label className="block font-bold">Keyword *<input value={form.keyword} onChange={e=>setForm(x=>({...x,keyword:e.target.value}))} required className="brutal-field mt-2 w-full"/></label><label className="block font-bold">Private DM message *<textarea value={form.dmMessage} onChange={e=>setForm(x=>({...x,dmMessage:e.target.value}))} required rows={5} className="brutal-field mt-2 w-full"/></label><label className="block font-bold">Public comment reply (optional)<textarea value={form.publicReplyMessage} onChange={e=>setForm(x=>({...x,publicReplyMessage:e.target.value}))} rows={5} className="brutal-field mt-2 w-full"/></label></div>{formError&&<p role="alert" className="mt-4 border-2 border-red-700 bg-red-50 p-3 text-red-800">{formError}</p>}<button type="submit" disabled={saving} className="brutal-button mt-5">{saving?"Creating rule…":"Create Rule"}</button></form>}<section className="mt-7"><div className="flex items-end justify-between gap-4"><div><p className="brutal-overline">Keyword rules</p><h2 className="mt-2 text-2xl font-black">Rules for @{accountName(accounts.find(a=>a.igUserId===selectedId))}</h2></div>{rulesError&&<button onClick={loadRules} className="font-black underline">Retry rules</button>}</div>{rulesLoading?<div className="brutal-card mt-5 animate-pulse p-8">Loading Comment Auto-DM rules…</div>:rulesError?<div role="alert" className="brutal-card mt-5 border-red-700 p-6 text-red-800"><h3 className="font-black">Rules couldn’t be loaded.</h3><p className="mt-2">{rulesError.message}{support(rulesError)}</p></div>:!rules.length?<div className="brutal-card mt-5 p-8 text-center"><h3 className="text-2xl font-black">No Comment Auto-DM rules yet.</h3><p className="mx-auto mt-3 max-w-xl text-zinc-600">Create a rule to send a private reply when someone comments a matching keyword on a selected Reel or post.</p>{canEdit&&<button onClick={()=>setShowForm(true)} className="brutal-button mt-6">Create Auto-DM Rule</button>}</div>:<div className="mt-5 grid gap-5 md:grid-cols-2">{rules.map(rule=><article key={rule.id} className="brutal-card min-w-0 p-5"><div className="flex items-start justify-between gap-3"><div><p className="brutal-overline">Keyword rule</p><h3 className="mt-2 break-words text-2xl font-black">“{rule.keyword}”</h3></div><span className={`border border-zinc-900 px-2 py-1 text-xs font-black ${rule.active===false?"bg-zinc-200":"bg-emerald-200"}`}>{rule.active===false?"INACTIVE":"ACTIVE"}</span></div><dl className="mt-5 space-y-4 text-sm"><div><dt className="font-bold text-zinc-500">Target Reel/Post</dt><dd className="break-all font-mono">{rule.mediaId}</dd></div><div><dt className="font-bold text-zinc-500">Private DM message</dt><dd className="mt-1 whitespace-pre-wrap border-2 border-zinc-900 bg-zinc-50 p-3">{rule.dmMessage}</dd></div><div><dt className="font-bold text-zinc-500">Public comment reply</dt><dd className="mt-1 whitespace-pre-wrap">{rule.publicReplyMessage||"Not configured"}</dd></div><div><dt className="font-bold text-zinc-500">Created</dt><dd>{rule.createdAt?new Date(rule.createdAt).toLocaleString():"—"}</dd></div></dl><Activity igUserId={selectedId} ruleId={rule.id} token={token} logout={logout}/>{canEdit&&<button onClick={()=>remove(rule)} disabled={deleting===rule.id} className="mt-4 border-2 border-red-700 bg-white px-4 py-2 font-black text-red-800">{deleting===rule.id?"Deleting…":"Delete rule"}</button>}</article>)}</div>}</section></>}</div></main>}
