import {Link} from "react-router-dom";import {useWorkspaceAuthorization} from "../context/WorkspaceAuthorizationContext";
export default function SettingsNav(){const {hasPermission}=useWorkspaceAuthorization();return <nav aria-label="Settings" className="flex flex-wrap gap-2">{[["Your profile","/profile",null],["Workspace profile","/settings/workspace","WORKSPACE_VIEW"],["Members","/settings/members","MEMBER_VIEW"],["Your invitations","/invitations",null],["Outreach templates","/settings/outreach-templates","OUTREACH_TEMPLATE_VIEW"]].filter(([, ,p])=>!p||hasPermission(p)).map(([label,to])=><Link key={to} className="rounded-full border border-panel-border bg-panel px-4 py-2 text-sm" to={to}>{label}</Link>)}</nav>}
